# torch is pre-installed in the ROCm container — do NOT reinstall it here
# PyTorch 2.9 dev + ROCm 7.0 is already present

transformers>=4.40.0
accelerate>=0.27.0
sentence-transformers>=2.7.0

# faiss-gpu has no ROCm wheel — cpu build is plenty fast for retrieval
faiss-cpu>=1.8.0

# Document ingestion
pypdf>=4.0.0
python-docx>=1.1.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Utilities
pydantic>=2.7.0
tqdm>=4.66.0
loguru>=0.7.2
python-dotenv>=1.0.0
numpy>=1.26.0
